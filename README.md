# character-battle-k8s-data-manifests

- `base/`: application backing services
- `jenkins/`: Jenkins controller bootstrap manifests
- `harbor/`: Harbor registry bootstrap manifests and Helm values
- `registry/`: lightweight Docker registry manifests
